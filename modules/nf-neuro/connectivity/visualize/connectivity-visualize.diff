Changes in component 'nf-neuro/connectivity/visualize'
'modules/nf-neuro/connectivity/visualize/environment.yml' is unchanged
'modules/nf-neuro/connectivity/visualize/meta.yml' is unchanged
Changes in 'connectivity/visualize/main.nf':
--- modules/nf-neuro/connectivity/visualize/main.nf
+++ modules/nf-neuro/connectivity/visualize/main.nf
@@ -7,7 +7,7 @@
         'scilus/scilus:2.0.2' }"
 
     input:
-    tuple val(meta), path(matrices), path (atlas_labels), path(labels_list)
+    tuple val(meta), path(matrices), path(atlas_labels), path(labels_list)
 
     output:
     tuple val(meta), path("*.png"), emit: figure
@@ -26,7 +26,6 @@
     def axis_text_size = task.ext.axis_text_size ? "--axis_text_size $task.ext.axis_text_size $task.ext.axis_text_size" : "--axis_text_size 5 5"
     def args = ""
     if (atlas_labels) args += [" --lookup_table $atlas_labels "]
-    if (labels_list) args += [" --name_axis "]
 
     """
     for matrix in $matrices_list; do
@@ -43,7 +42,7 @@
     """
 
     stub:
-    String  matrices_list = matrices.join(", ").replace(',', '')
+    String matrices_list = matrices.join(", ").replace(',', '')
     """
     for metric in $matrices_list; do
         base_name=\$(basename "\${metric}" .npy)

'modules/nf-neuro/connectivity/visualize/tests/main.nf.test.snap' is unchanged
'modules/nf-neuro/connectivity/visualize/tests/tags.yml' is unchanged
'modules/nf-neuro/connectivity/visualize/tests/nextflow.config' is unchanged
'modules/nf-neuro/connectivity/visualize/tests/main.nf.test' is unchanged
************************************************************
